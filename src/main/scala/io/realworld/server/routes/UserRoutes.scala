package io.realworld.server.routes

import zio._
import zhttp.http._
import zio.json._

object UserRoutes {
//  val routes: Http[Any, Throwable, Request, Response] = Http.collectZIO[Request] {
//
//    case Method.GET -> !! / "api" / "user" =>
//      for {
//
//        profile <- ZIO.succeed(UserResponse(UserDto(loginRequest.user.email, AuthToken("token"), Username("username"))))
//      } yield Response.json(user.toJson)
//
//  }
}
